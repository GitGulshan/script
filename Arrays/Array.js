/*

// Array - Variables that gold multiple values

const num = new Array(1, 2, 3, 4, 5);

console.log(num);

const color = ["red", "blue", "green", 10, null]; // we can you use multiples data types. // js is not a statically typed language.

console.log(color);

const fruits = ["banana", "fish", "chicken", "ball"];
console.log(fruits);

console.log(fruits[4]); // undefined.

fruits[4] = "cars"; // not a good practices.

console.log(fruits[1]);
console.log(fruits);

fruits.push("audi", "r8"); // this add the new elements in the end
console.log(fruits);

fruits.unshift("bikes", "ducati"); // this add the new elements in the begginng.
console.log(fruits);

fruits.pop(fruits); // it delets the last elements from the array
console.log(fruits);

console.log(Array.isArray(fruits)); // check somthing is in array
console.log(Array.isArray()); // check somthing is in array-----------

console.log(fruits.indexOf()); /// blank me -1 kuyu diya result------

console.log(fruits.indexOf("audi")); // it return the value of the elments.

console.log(fruits.lastIndexOf("ball"));
console.log(fruits.lastIndexOf("bikes")); // difference kya huwa dono me---------

*/
